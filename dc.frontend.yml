version: "3.4"

services:
  frontend:
    container_name: tri-frontend
    image: vcheeney/tri-frontend
    build: ./frontend
    environment:
      - VITE_API_BASE_PATH=http://localhost:3000
    ports:
      - "5000:5000"
